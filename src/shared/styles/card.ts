import { css } from "lit";

export const cardStyles = css`
  .card-content {
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .chips {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }
`;
